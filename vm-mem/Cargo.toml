[package]
name = "vm-mem"
version = "0.1.0"
edition = "2024"
edition = "2024"

[dependencies]
vm-core = { path = "../vm-core" }
memmap2 = { version = "0.9", optional = true }
libc = "0.2"
lru = "0.12"
parking_lot = "0.12.5"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "mmu_translate"
harness = false

[features]
memmap = ["memmap2"]
no_std = []
