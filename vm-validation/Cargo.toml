[package]
name = "vm-validation"
version = "0.1.0"
edition = "2021"
description = "Common validation framework for VM project"

[dependencies]
vm-common = { path = "../vm-common" }
vm-error = { path = "../vm-error" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
log = "0.4"
regex = "1.0"

[features]
default = ["std"]
std = []
no_std = []