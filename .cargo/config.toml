# Cargo 配置文件
# 设置测试超时和并发策略

# 注意：不设置 jobs 参数时，Cargo 会自动使用所有可用CPU核心并行构建和测试
# 这是最优配置，可以最大化利用多核CPU提升构建和测试速度

[test]
# 测试超时配置（秒）
# 注意：Cargo本身不支持测试超时，需要在测试代码中实现
timeout = 300  # 5分钟（仅作为文档说明，实际需要在代码中实现）

# 测试输出配置
quiet = false
nocapture = false
