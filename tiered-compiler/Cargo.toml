[package]
name = "tiered-compiler"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
parking_lot = "0.12"
vm-runtime = { path = "../vm-runtime" }
# async-executor 和 coroutine-scheduler 已合并到 vm-runtime

[dev-dependencies]
