# 阶段 1：依赖升级计划

## 目标
升级所有过时依赖到最新稳定版本，提升性能和安全性

## 优先级
🔴 **P0 - 高优先级**（影响性能和安全性）

## 时间估算
1-2 周

## 问题概述

### 依赖版本状态

#### 严重过时（需立即升级）

| 依赖 | 当前版本 | 最新稳定版本 | 版本差距 | 影响 |
|------|----------|-------------|----------|------|
| **Cranelift 系列** | **=0.110.3** | **0.127.2** | **17 小版本** | 性能损失 5-10%，缺少最新特性 |

#### 轻微过时（建议升级）

| 依赖 | 当前版本 | 最新稳定版本 | 版本差距 | 影响 |
|------|----------|-------------|----------|------|
| tokio | 1.48 | 1.49 | 1 小版本 | 稳定性小幅提升 |
| raw-cpuid | 11 | 11.2 | 0.2 小版本 | CPU 特性检测改进 |

#### 版本不一致（需统一）

| 依赖 | 工作区版本 | 实际使用版本 | 差异 | 影响 |
|------|----------|-------------|------|------|
| lru | 0.12 | 0.16 (vm-mem) | 不一致 | 可能构建警告 |
| criterion | 0.5 | 0.8 (dev-deps) | 不一致 | 基准测试不一致 |

## 实施步骤

### 步骤 1：升级 Cranelift（3-4 天）

#### 1.1 Cranelift 版本研究（1 天）

**任务**：
- [ ] 研究 Cranelift 0.110.3 → 0.127.2 的变更日志
- [ ] 识别破环性变更（Breaking Changes）
- [ ] 评估兼容性影响

**信息源**：
- [Cranelift GitHub Releases](https://github.com/bytecodealliance/wasmtime/releases)
- [Cranelift Changelog](https://github.com/bytecodealliance/wasmtime/blob/main/cranelift/CHANGELOG.md)
- [Cranelift Migration Guides](https://docs.rs/cranelift/)

**预期发现**：
- API 变更
- 新增特性
- Bug 修复
- 性能改进

**输出**：
```markdown
# Cranelift 升级调研报告

## 版本变更
- 0.110.3 → 0.111.x
- 0.111.x → 0.112.x
- ...
- 0.126.x → 0.127.2

## 破坏性变更
1. ...
2. ...

## 需要修改的代码
- crates/execution/vm-engine-jit/src/cranelift_backend.rs
- crates/execution/vm-engine-jit/src/codegen.rs
- ...

## 风险评估
- 高风险：...
- 中风险：...
- 低风险：...
```

**验证标准**：
- [ ] 变更日志已收集
- [ ] 破坏性变更已识别
- [ ] 风险评估完成

#### 1.2 更新 workspace 依赖配置（30 分钟）

**任务**：
- [ ] 更新根 Cargo.toml 的 Cranelift 版本

**修改前**：
```toml
# Cargo.toml (workspace)
[workspace.dependencies]
# Cranelift (JIT) - Pinned to 0.110.3 for stability and compatibility
cranelift-codegen = "=0.110.3"
cranelift-frontend = "=0.110.3"
cranelift-module = "=0.110.3"
cranelift-native = "=0.110.3"
cranelift-control = "=0.110.3"
```

**修改后**：
```toml
# Cargo.toml (workspace)
[workspace.dependencies]
# Cranelift (JIT) - Upgraded to 0.127.2 for performance and features
cranelift-codegen = "0.127"
cranelift-frontend = "0.127"
cranelift-module = "0.127"
cranelift-native = "0.127"
cranelift-control = "0.127"
```

**重要变更**：
- ❌ **移除** `=` 锁定符（允许兼容的小版本更新）
- ✅ **使用** `"0.127"`（允许 0.127.x 更新）
- ✅ **考虑** `"0.127"` → `"0.127.2"`（如果需要精确版本）

**验证标准**：
- [ ] workspace 配置已更新
- [ ] 版本号正确

#### 1.3 更新各 crate 的 Cranelift 引用（1 小时）

**需要更新的文件**：

| Crate | 文件路径 | 当前引用 | 新引用 |
|-------|----------|----------|--------|
| vm-engine-jit | crates/execution/vm-engine-jit/Cargo.toml | `=0.110.3` | `{ workspace = true }` |
| vm-engine | crates/execution/vm-engine/Cargo.toml | `{ workspace = true }` | `{ workspace = true }`（保持） |

**vm-engine-jit/Cargo.toml 修改**：
```toml
# 修改前
[dependencies]
cranelift = "=0.110.3"
cranelift-codegen = "=0.110.3"
cranelift-frontend = "=0.110.3"
cranelift-module = "=0.110.3"
cranelift-jit = "=0.110.3"
cranelift-native = "=0.110.3"

# 修改后
[dependencies]
cranelift = { workspace = true }
cranelift-codegen = { workspace = true }
cranelift-frontend = { workspace = true }
cranelift-module = { workspace = true }
cranelift-jit = { workspace = true }
cranelift-native = { workspace = true }
```

**验证标准**：
- [ ] 所有引用已更新
- [ ] 使用 workspace 依赖
- [ ] 版本号移除

#### 1.4 修复 API 破坏性变更（2-3 天）

**可能的破坏性变更**：

##### 变更 1：Cranelift API 变更
```rust
// 修改前（0.110.3）
use cranelift_codegen::Context;
let mut ctx = Context::new();

// 修改后（0.127.2）
use cranelift_codegen::Context;
let mut ctx = Context::for_module(ModuleFlags::default());
// 或
let mut ctx = Context::new();
```

##### 变更 2：编码器 API
```rust
// 修改前
let mut builder = FunctionBuilder::new(&mut ctx);

// 修改后
let mut builder = FunctionBuilder::new(&mut ctx, Signature::new(Lookup::default()));
```

##### 变更 3：后端配置
```rust
// 修改前
let backend = Backend::new(&mut ctx);

// 修改后
let backend = Backend::new(&mut ctx, None);
```

**任务清单**：
- [ ] 修复 `cranelift_backend.rs` 中的 API 调用
- [ ] 修复 `codegen.rs` 中的 API 调用
- [ ] 修复 `optimizer.rs` 中的 API 调用
- [ ] 更新类型定义（如需要）
- [ ] 修复 trait 实现（如需要）

**验证标准**：
- [ ] 所有编译错误已修复
- [ ] API 使用符合最新文档

#### 1.5 编译测试（1 天）

**任务**：
- [ ] 尝试编译受影响的 crates
- [ ] 修复编译错误

**命令**：
```bash
# 清理构建缓存
cargo clean

# 编译 vm-engine-jit
cargo build -p vm-engine-jit

# 编译 vm-engine
cargo build -p vm-engine

# 编译所有 workspace
cargo build --workspace
```

**预期编译错误**：
- 类型不匹配
- 方法不存在
- trait 未实现

**验证标准**：
- [ ] 编译成功（零错误）
- [ ] 所有 crate 可构建

#### 1.6 测试运行（1 天）

**任务**：
- [ ] 运行 JIT 相关测试
- [ ] 验证功能正确性
- [ ] 性能基准测试

**命令**：
```bash
# 运行 vm-engine-jit 测试
cargo test -p vm-engine-jit

# 运行 vm-engine 测试
cargo test -p vm-engine

# 运行性能基准
cargo bench -p vm-engine-jit
cargo bench -p vm-engine
```

**验证标准**：
- [ ] 所有测试通过
- [ ] 性能未显著下降
- [ ] 功能正常

#### 1.7 性能验证（半天）

**任务**：
- [ ] 运行性能基准测试
- [ ] 对比升级前后的性能
- [ ] 确认性能提升

**性能指标**：
- JIT 编译时间（per block）
- 代码质量（执行速度）
- 缓存命中率
- 内存占用

**验证标准**：
- [ ] 性能提升 5-10%（预期）
- [ ] 无性能回归

### 步骤 2：升级 Tokio（半天）

#### 2.1 更新 workspace 配置（15 分钟）

**修改**：
```toml
# Cargo.toml (workspace)
# 修改前
tokio = { version = "1.48", features = ["sync", "rt", "rt-multi-thread", "macros", "time", "io-util", "fs"] }

# 修改后
tokio = { version = "1.49", features = ["sync", "rt", "rt-multi-thread", "macros", "time", "io-util", "fs"] }
```

**验证标准**：
- [ ] Tokio 版本已更新

#### 2.2 测试异步功能（1-2 小时）

**任务**：
- [ ] 编译异步相关 crates
- [ ] 运行异步测试
- [ ] 验证异步运行时正常

**命令**：
```bash
# 编译
cargo build --workspace --features async

# 测试
cargo test --workspace --features async
```

**验证标准**：
- [ ] 异步功能正常
- [ ] 无新警告或错误

### 步骤 3：统一版本不一致（半天）

#### 3.1 统一 lru 版本（30 分钟）

**任务**：
- [ ] 更新 vm-mem 的 lru 引用

**vm-mem/Cargo.toml 修改**：
```toml
# 修改前
[dependencies]
lru = "0.16"

# 修改后
[dependencies]
lru = { workspace = true }
```

**验证标准**：
- [ ] lru 版本统一到 workspace 版本（0.12）

#### 3.2 统一 criterion 版本（30 分钟）

**任务**：
- [ ] 更新所有 dev-dependencies 中的 criterion

**修改**：
```toml
# 修改前（某些 crate）
[dev-dependencies]
criterion = "0.8"

# 修改后（所有 crate）
[dev-dependencies]
criterion = { workspace = true }
```

**验证标准**：
- [ ] criterion 版本统一到 workspace 版本（0.5）

### 步骤 4：升级 raw-cpuid（15 分钟）

**任务**：
- [ ] 更新 raw-cpuid 版本

**修改**：
```toml
# 修改前
raw-cpuid = { version = "11", optional = true }

# 修改后
raw-cpuid = { version = "11.2", optional = true }
```

**验证标准**：
- [ ] raw-cpuid 版本已更新

### 步骤 5：全面测试（1 天）

#### 5.1 编译所有 workspace（2 小时）

**命令**：
```bash
# 清理
cargo clean

# 编译所有
cargo build --workspace

# 编译所有特性组合
cargo build --workspace --all-features
```

**验证标准**：
- [ ] 所有 crate 可编译
- [ ] 所有特性组合成功

#### 5.2 运行所有测试（2 小时）

**命令**：
```bash
# 运行所有测试
cargo test --workspace

# 运行所有特性组合测试
cargo test --workspace --all-features
```

**验证标准**：
- [ ] 所有测试通过
- [ ] 无新测试失败

#### 5.3 Clippy 检查（1 小时）

**命令**：
```bash
# Clippy 检查
cargo clippy --workspace --all-features -- -D warnings
```

**验证标准**：
- [ ] 无 Clippy 错误
- [ ] Clippy 警告可接受（或记录待修复）

## 成功标准

### 阶段完成标准
- [ ] ✅ Cranelift 升级到 0.127.2
- [ ] ✅ Tokio 升级到 1.49
- [ ] ✅ 所有依赖版本统一
- [ ] ✅ 所有测试通过
- [ ] ✅ 无新增编译错误

### 性能标准
- [ ] ✅ JIT 编译速度提升 5-10%
- [ ] ✅ JIT 代码质量提升 5-10%
- [ ] ✅ 无性能回归（<2%）

### 质量标准
- [ ] ✅ 所有代码符合 Cranelift 最新 API
- [ ] ✅ 无已弃用的 API 使用
- [ ] ✅ 所有警告已审查

## 回滚计划

如果升级导致严重问题：

### 回滚步骤 1：版本回滚
```bash
# 回滚 Cranelift 到 0.110.3
git revert <commit-hash>

# 或手动修改版本号
# 使用 git stash 保存工作
```

### 回滚步骤 2：验证回滚
```bash
# 验证项目可构建
cargo build --workspace

# 验证测试通过
cargo test --workspace
```

### 回滚步骤 3：分析失败原因
- [ ] 记录具体失败
- [ ] 分析 root cause
- [ ] 制定新的升级策略

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| Cranelift API 破坏性变更导致大量代码修改 | 高 | 高 | 提前调研，逐步迁移，保留备份 |
| 性能回归（意外变慢） | 中 | 中 | 性能基准测试，详细性能对比 |
| 测试失败（API 变更导致） | 中 | 高 | 详细测试，修复测试 |
| 兼容性问题（与平台相关） | 低 | 中 | 多平台测试，条件编译测试 |
| 依赖冲突（transitive dependencies） | 低 | 中 | 使用 `cargo update -p <crate>` 精确控制 |

## 时间分配

| 任务 | 预计时间 | 实际时间 | 状态 |
|------|----------|----------|------|
| 步骤 1：升级 Cranelift | 3-4 天 | | ☐ |
| 步骤 2：升级 Tokio | 半天 | | ☐ |
| 步骤 3：统一版本不一致 | 半天 | | ☐ |
| 步骤 4：升级 raw-cpuid | 15 分钟 | | ☐ |
| 步骤 5：全面测试 | 1 天 | | ☐ |
| **总计** | **5-6 天** | | **待开始** |

## 后续行动

阶段 1 完成后：

### 立即行动
- [ ] 提交依赖升级到版本控制
- [ ] 创建 PR 供代码审查
- [ ] 更新 CHANGELOG.md

### 进入阶段 2
- [ ] 开始代码质量清理计划
- [ ] 执行阶段 2：代码质量清理

## 附录

### A. Cranelift 版本历史（简化）

| 版本 | 发布日期 | 主要变更 |
|------|----------|----------|
| 0.110.3 | 2024-XX-XX | 当前版本 |
| 0.111.0 | ... | ... |
| ... | ... | ... |
| 0.127.0 | 2025-XX-XX | 性能改进 |
| 0.127.2 | 2025-XX-XX | Bug 修复 |

### B. 参考资料
- [Cranelift Documentation](https://docs.rs/cranelift/)
- [Tokio Documentation](https://docs.rs/tokio/)
- [Cargo Update Guide](https://doc.rust-lang.org/cargo/commands/cargo-update.html)

---

**文档版本**：1.0  
**创建日期**：2026-01-20  
**负责人**：待分配  
**状态**：待开始
